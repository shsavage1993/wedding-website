import React from 'react';
import { FC } from 'react';
import { CodeProps } from '../model';

export const GalleryPage: FC<CodeProps> = ({ code, sgp, cph }) => {
	return (
		<div style={{ marginTop: '76px' }}>
			<h1>Gallery</h1>
		</div>
	);
};
